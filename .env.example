# OCLite Extension — Example Environment Variables
# Copy this file to .env and fill in your values
# NEVER commit .env to version control!

# ── These keys are now stored in VS Code SecretStorage ──
# Set them via the Command Palette:
#   OCLite: Set API Key          → your sk-oclite-xxx key from https://oclite.site
#   OCLite: Set Chat Key         → your Azure Function ?code= value
#   OCLite: Clear All API Keys   → removes all stored keys
#
# You do NOT need a .env file for normal usage.
