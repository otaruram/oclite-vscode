node_modules
out
dist
.vscode-test
*.vsix

# Environment files with secrets
.env
.env.*
.env.local
.env.*.local

# LLM gateway with real keys (use llm.ts.example as template)
src/services/llm.ts

# Secrets module (encrypted secrets — NEVER push the source)
src/utilities/secrets.ts

# OS files
.DS_Store
Thumbs.db

# Editor settings (may contain tokens)
.vscode/settings.json
